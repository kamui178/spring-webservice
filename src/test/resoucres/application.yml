#Test

spring:  profiles:    active: local # Base environment
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect   
---    
#Local

spring:
  profiles: local
  jpa:
    show-sql : true